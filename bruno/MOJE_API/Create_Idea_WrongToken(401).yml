info:
  name: Create_Idea_WrongToken(401)
  type: http
  seq: 14

http:
  method: POST
  url: http://localhost:3000/ideas
  body:
    type: json
    data: |-
      {
        "title": "Build my own API",
        "description": "My first idea stored in Postgres",
        "status": "ready"
      }
  auth:
    type: bearer
    token: MSKJJDJOJWD93904I

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 401", function () {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
