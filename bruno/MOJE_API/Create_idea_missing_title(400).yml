info:
  name: Create_idea_missing_title(400)
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/ideas
  body:
    type: json
    data: |-
      { 
      "description": "x"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Rejects missing title with 400", function () {
          expect(res.getStatus()).to.equal(400);
        });

        test("Explains which field failed", function () {
          const body = res.getBody();
          expect(body.details[0].field).to.equal("title");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
