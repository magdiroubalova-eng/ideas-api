info:
  name: Delete_idea
  type: http
  seq: 8

http:
  method: DELETE
  url: http://localhost:3000/ideas/2
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 204", function () {
          expect(res.getStatus()).to.equal(204);
        });

        test("Response has no body", function () {
          expect(res.getBody()).to.not.be.ok; // null, undefined, or empty string all pass
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
